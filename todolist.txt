revit ui:
	fix revit ui items
	don't add separator if it is at the end


Cache:
	clean cache files


parser: - get config script file name from __config__?
	- add context script __context__ (IExternalCommandAvailability )
	lib packages


script info:
	implement the two functions


user config:

	disable caching
	disabling tools
	custom user settings

Legacy scripts:
	about with WPF xml
	settings with WPF xml.

loader: implement/cleanup command options
	command options must be dynamic if handled by loader

Pkg manager:
	window to list available packages, allows installation and removal
	update is handled through main update module

compiling ironpython for speed:
https://blogs.msdn.microsoft.com/srivatsn/2008/08/06/static-compilation-of-ironpython-scripts/