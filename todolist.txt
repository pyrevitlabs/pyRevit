revit ui:
	fix revit ui items

Cache:
	clean cache files

Pkg manager:
	window to list available packages, allows installation and removal
	update is handled through main update module

script info:
	implement the three functions

user config:
	disable caching
	disabling tools
	custom user settings

Legacy scripts:
	about with WPF xml
	settings with WPF xml.

Parser:
	add support for adding panels to other packages: currently works because of name similarity in ui module

