# known tools
# Flat layout-mode unique name (extensionname_toolname) for tools/Reload.pushbutton.
# pyRevitCore loads via extension_layout.yaml, so the command class is named
# from the tool alone, not its legacy ribbon path.
PYREVIT_CORE_RELOAD_COMMAND_NAME = 'pyrevitcore_reload'
