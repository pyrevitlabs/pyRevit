# # pylint: skip-file
# import os.path as op
# from pyrevit import  USER_DESKTOP

# with open(op.join(USER_DESKTOP, 'hooks.txt'), 'a') as f:
#     f.write('\n'.join([
#         'Application Idle '.ljust(80, '-'),
#         'Cancellable? ' + str(__eventargs__.Cancellable) + '\n']))
